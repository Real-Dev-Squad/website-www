<Tabs
  data-test-live='header'
  class='live__header'
  {{on 'click' this.tabHandler}}
>
  {{#each this.TABS as |tab|}}
    <Tab
      @test={{tab.label}}
      @label={{tab.label}}
      @variant={{if tab.active 'active' ''}}
    />
  {{/each}}
</Tabs>
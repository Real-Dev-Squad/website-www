<div class='logo logos-container__rds' data-test-rds-logo>
{{#if this.login.isLoggedIn}}
     <img
      src='assets/icons/rds-github-link.png'
      alt='RDS-Logo'
      class='logo__github-rds'
      data-test-rds-logo-img
    />
  <p class="signup-page-heading">Thank you for connecting your GitHub</p>
  <p class="p2-description">Please continue with Sign Up with in order to use features display yourself on members page etc</p>
  <Reusables::Button
    @text="Let's Go"
    @variant='dark'
    @onClick={{@letsGoHandler}}
    @test='start'
    @type='button'
  />
  {{else if  (not this.login.isLoggedIn)}}
  <p class="signup-page-heading">You are not logged in. Please log in to continue...</p>
  <Reusables::Button
    @text='login'
    @variant='dark'
    @onClick={{this.logingithub}}
    @test='start'
    @type='button'
  />
{{/if}}
  </div>
